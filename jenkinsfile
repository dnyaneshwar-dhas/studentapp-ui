pipeline {
    agent {
        label 'worker'
    }

    stages {
        stage('checkout') {
            steps {
                sh '''
                echo "clonning is successful"
                '''
            }
        }
    }
}