pipeline {
    agent {
        label 'worker'
    }

    stages {
        stage('checkout') {
            steps {
                sh '''
                echo "cloning is sucessfull
                '''
            }

        }
    }
}