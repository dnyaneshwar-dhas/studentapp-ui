pipeline {
    agent {
        label 'worker'
    }

    stages {
        stage('checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/mayurpandit25/studentapp-ui.git'
            }
        }
    }
}